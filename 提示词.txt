任务：无人机配送路径规划项目，优先跑通基线模型最小可行版本
开发要求：请用 Python 完成：仿真数据生成 + 数据清洗 + 距离矩阵 + 贪心算法基线 + 结果输出 + 可视化，只做最简可用基线版本，先跑通、出结果、能复现
全部代码均在drone_delivery环境中运行，不要污染全局环境
一、技术栈（必须用）
数值计算：NumPy
可视化：Matplotlib
二、数据生成（仿真，无外部数据）
客户点：用numpy.random生成 20 个平面坐标，范围 [0,100]×[0,100]
每个客户点：随机分配包裹重量 1–5kg
无人机：3 架，固定参数
单架最大载重：20kg
单架最大飞行里程：200 单位距离
数据可复现：设置随机种子
数据清洗：剔除坐标异常、重复点、重量异常，输出干净数据集
三、距离矩阵
基于客户坐标，计算两两欧氏距离，生成距离矩阵
四、基线算法：贪心算法
目标：总飞行距离最短
硬约束：
单架载重 ≤ 20kg
单架里程 ≤ 200
所以飞机飞完一轮后若还有未配送的客户，则再开启一轮，直到所有客户点被覆盖
输出：
总飞行距离
实际使用无人机数量
每架无人机：路径顺序、载重、里程、配送次数
五、可视化与输出
Matplotlib 绘制：客户点、配送中心、每架无人机路径（不同颜色）
打印：关键指标 + 各路径详情
代码：结构清晰、中文注释完整、可直接运行、无报错、结果可复现
六、交付要求
只完成基线最简版：数据生成→清洗→距离矩阵→贪心路径规划→结果 + 可视化；不要做模拟退火、遗传算法，为后续调优留空间。